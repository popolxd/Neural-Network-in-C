#include "neural.h"

void one_propagation_forward(double*** weights, double** biases, double* first_input, int num_of_layers, int* dimensions) { // num of layers without input

    for (int i = 0; i < num_of_layers; i++) {

        double result[dimensions[i + 1]];

        // matrix_vector_mult(weights[i], )

    }
}